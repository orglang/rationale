# Что такое трансакция

> *"Видишь суслика? — Нет. — И я не вижу. А он есть!"*
> <p align="right">— ДМБ (2000)</p>

Попробуем увидеть ~~суслика~~трансакцию на айтишных примерах, хоть это и будет непросто.

> [!WARNING]
> Несмотря на то, что *транзакция* является устоявшимся понятием в IT-индустрии, мы будем использовать очень схожий термин *трансакция*, но в абсолютно другом значении.

## Объяснение

Начнем с примера, в котором точно нет трансакций.

```go
func DoSomeWork() WorkOutcome {
    outcome := DoFirstTask()
    return DoSecondTask(outcome)
}
```
<p align="center"><sub>Листинг 1. Чистая работа</sub></p>

Считаем, что `DoFirstTask` и `DoSecondTask` являются чистыми функциями без побочных эффектов.

> [!IMPORTANT]
> Нет побочных эффектов => нет трансакций

Рассмотрим следующий пример.

```go
func DoSomeWork() WorkOutcome {
    provider := NewWorkProvider()
    outcome := provider.DoFirstTask()
    return provider.DoSecondTask(outcome)
}
```
<p align="center"><sub>Листинг 2. Работа через провайдера</sub></p>

В примере из листинга 1 функция `DoSomeWork` была чистой. Теперь же в рамках неё создается провайдер, которому делегируется выполнение работы. Данная операция является побочным эффектом, т.к. при её выполнении аллоцируются вычислительные ресурсы. Можно ли такого рода операцию назвать трансакцией? Ответ зависит от реализации функции `NewWorkProvider`.

Для яркости примера будем считать, что при создании провайдера аллоцируются сотни гигабайт оперативной памяти.

Если свободная оперативная память уже в наличии или она добавляется автоматически по запросу, то трансакции нет. Это значит, что всё случилось до начала работы программы.

Если в рамках вызова `NewWorkProvider` (чисто гипотетически) программа приостанавливается, ищется владелец требуемого количества оперативной памяти, выполняется обоснование, фиксируются договоренности, память физически добавляется на машину, затем программа возобновляется, то это и есть трансакция.

> [!IMPORTANT]
> Аллокация ранее согласованных ресурсов не является трансакцией.

Трансакция - это процедура получения доступа к ресурсу, процедура вовлечения ресурса в работу. Обычно такая процедура осуществляется до начала работы программы.

Основная проблема с трансакциями в том, что они несут издержки. Трансакция - это как правило ручные действия, а ручные действия чаще всего ведут к очень большим издержкам (еще рискам и неопределенностям, но об этом как-нибудь в другой раз).

Проблема понятна, но как её решать?

Нужен инструмент, который позволит увидеть трансакции, как микроскоп позволяет увидеть микромир. Это как минимум даст возможность посчитать трансакционные издержки, поставить их на учет. Также нужен инструмент, который позволит замещать тяжеловесные трансакции с участием людей на легковесные трансакции с участием ботов.

> [!IMPORTANT]
> Orglang является инструментом, который позволяет увидеть трансакции, посчитать их издержки, спроектировать и заместить их более легковесными аналогами.

В идеале все трансакции выполняются в автоматическом режиме без ручного вмешательства (снятие трансакций). Трансакционные издержки снижаются на порядки. Вместе с этим уходят сопутствующие им риски и неопределенности.

## Формализация

В качестве формальной модели возьмем теорию сессионных типов [^1]. В рамках теории существует операция порождения нового процесса, которая является аналогом оператора `new` в языках программирования.

При порождении нового процесса аллоцируется ресурс, который мы будем называть *производительностью*. Когда процесс *P* порождает процесс `Q` аллоцируется производительность `E`. Что значит аллокация производительности? Новый процесс `Q` попадает в *пул*, который обладает свободной производительностью для его исполнения.

Для исполнения процесса `Q` пулу может потребоваться процесс `R`, для порождения которого требуется обращение к другому пулу. Коммуникацию двух пулов по поводу аллокации производительности будем называть *трансакцией*.

Формально пул очень похож на процесс.
```
x₁ : A₁,...,xₙ : Aₙ ⊢ P :: (z : B)
```
<p align="center"><sub>Листинг 3. Typing judgement</sub></p>

Пул *P* может *использовать* каналы *x*<sub>*1*</sub>,...,*x*<sub>*𝚗*</sub> для коммуникации с другими пулами по протоколам *A*<sub>*1*</sub>,...,*A*<sub>*𝚗*</sub> соответственно и *предоставляет* канал *z* с протоколом *B*, через который с ним могут коммуницировать другие пулы.

Канал может быть *реплицируемым* или *линейным*. В рамках коммуникации пул может выполнить публикацию *значения* `V` или подписку *продолжения* `K`.

| Значение *V*  | Семантика |
| :-----------  | :-------- |
| ⟨⟩          | Терминальное значение без какой-либо полезной информации. |
| shift(*c*<sub>*𝚔*</sub>) | Канал *c*<sub>*𝚔*</sub> в режиме *𝚔*, помеченный как shift. |
| choice(*c*) {*𝚕*}   | Канал *c*, помеченный как choice, с информацией класса *𝚕*. |
| coop(*c*) {*q*<sub>*𝚠*&nbsp;</sub>} | Канал *c*, помеченный как coop, с долей *𝚠* в производительности по именованному процессу *q*. |
| isol(*c*) {*q*<sub>*𝚠*&nbsp;</sub>} | Канал *c*, помеченный как isol, с долей *𝚠* в производительности по именованному процессу *q*. |
| call(*c*)&nbsp;{*a*&nbsp;←&nbsp;*q*&nbsp;*b<sub>*1*</sub>*...*b<sub>*𝚗*&nbsp;</sub>*} | Канал *c*, помеченный как call, с вызовом именованного процесса *q* и аргументами *a*, *b<sub>*1*</sub>*,...,*b<sub>*𝚗*&nbsp;</sub>*. |
| spawn(*c*) {*q*} | Канал *c*, помеченный как spawn, с порождением именованного процесса *q*. |

<p align="center"><sub>Таблица 1. Значения</sub></p>

| Продолжение *K* | Семантика |
| :-------------- | :-------- |
| (⟨⟩ ⇒ *P*) | При получении терминального значения *⟨⟩* продолжить как *P*. |
| (shift(*x*<sub>*𝚔*</sub>) ⇒ *P*) | При получении значения shift(*c*<sub>*𝚔*</sub>) продолжить как *P*[*c*<sub>*𝚔*&nbsp;</sub>/*x*<sub>*𝚔*&nbsp;</sub>]. Происходит связывание переменной *x*<sub>*𝚔*</sub> в рамках терма *P*. |
| (choice(*x*<sub>*𝚓*</sub>) {*𝚓*} ⇒ *P*<sub>*𝚓*</sub>)<sub>*𝚓* ∈ *𝙹*</sub> | При получении значения choice(*c*){*𝚕*} для некоторого *𝚕* ∈ *𝙹* продолжить как *P*<sub>*𝚕*&nbsp;</sub>[*c*/*x*<sub>*𝚓*</sub>]. Происходит связывание переменной *x*<sub>*𝚓*</sub> в рамках терма *P*<sub>*𝚓*</sub> для каждого *𝚓* ∈ *𝙹*. |
| (coop(*x*) {*q*<sub>*𝚠*&nbsp;</sub>} ⇒ *P* : *T*) | При получении значения coop(*c*){*q*<sub>*𝚠*&nbsp;</sub>} продолжить как *P*[*c*/*x*] : [*Q* + {*q*<sub>*𝚠*&nbsp;</sub>}]*T*. Происходит связывание переменной *x* в рамках терма *P* и наращивание доли на величину *𝚠* в производительности по *q*. |
| (isol(*x*) {*q*<sub>*𝚠*&nbsp;</sub>} ⇒ *P* : *T*) | При получении значения isol(*c*){*q*<sub>*𝚠*&nbsp;</sub>} продолжить как *P*[*c*/*x*] : [*Q* - {*q*<sub>*𝚠*&nbsp;</sub>}]*T*. Происходит связывание переменной *x* в рамках терма *P* и сокращение доли на величину *𝚠* в производительности по *q*. |
| (call(*x*)&nbsp;{*a*&nbsp;←&nbsp;*q*&nbsp;*b*<sub>*1*</sub>...*b*<sub>*𝚗*&nbsp;</sub>}&nbsp;⇒&nbsp;*P*) | При получении значения spawn(*c*){*q*} для некоторого *q* ∈ *Q* продолжить как *P*[*c*/*x*]. Происходит связывание переменной *x* в рамках терма *P*. |
| (spawn(*x*) {*q*} ⇒ *P*) | При получении значения call(*c*){*a* ← *q* *b*<sub>*1*</sub>...*b*<sub>*𝚗*&nbsp;</sub>} для некоторого *q* ∈ *Q* продолжить как *P*[*c*/*x*]. Происходит связывание переменной *x* в рамках терма *P*. |

<p align="center"><sub>Таблица 2. Продолжения</sub></p>

[^1]: Luis Caires and Frank Pfenning. 2010. Session Types as Intuitionistic Linear Propositions.  
